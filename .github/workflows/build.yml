name: Generate .apk file

on:
  pull_request:
    branches:
      - develop

jobs:
  setup-env:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install React Native
        run: yarn global add react-native

      - name: Set up Java 11
        uses: actions/setup-java@v2
        with:
          java-version: "11"
          distribution: "temurin"
          cache: gradle

      - name: Setup Android SDK
        uses: android-actions/setup-android@v2

      - name: Use Node.js 14
        uses: actions/setup-node@v3
        with:
          node-version: 16.18.0
          cache: "yarn"

      - name: Install Node modules
        run: yarn

      - name: Cache Gradle Wrapper
        uses: actions/cache@v2
        with:
          path: ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-wrapper-${{ hashFiles('gradle/wrapper/gradle-wrapper.properties') }}

      - name: Cache Gradle Dependencies
        uses: actions/cache@v2
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-caches-${{ hashFiles('gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-caches-

      - name: Make Gradlew Executable
        run: cd android && chmod +x ./gradlew

  build-android:
    needs: setup-env
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Make release.keystore
        run: |
          cd android/app && cp debug.keystore release.keystore && cd ../..

      - name: Check assets folder exist or not
        id: check_folder
        uses: andstor/file-existence-action@v1
        with:
          files: "android/app/src/main/assets"

      - name: File exists
        if: steps.check_folder.outputs.files_exists == 'false'
        run: |
          mkdir android/app/src/main/assets

      - name: Build Android Release
        run: |
          cd android && ./gradlew assembleRelease --no-daemon

      - name: Upload Artifact
        uses: actions/upload-artifact@v1
        with:
          name: app-release.apk
          path: android/app/build/outputs/apk/release/
